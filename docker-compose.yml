version: "3.8"

services:
  flowcrm:
    image: nginx:alpine
    container_name: flowcrm
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - Vizionsnet

networks:
  Vizionsnet:
    external: true
